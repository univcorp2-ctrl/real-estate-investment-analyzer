{
  "name": "real-estate-investment-analyzer",
  "version": "0.1.0",
  "description": "Prototype real estate investment scoring and cash-flow analyzer based on public behavior inference.",
  "type": "commonjs",
  "scripts": {
    "dev": "npx http-server . -p 4173 -c-1",
    "test": "node --test"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
